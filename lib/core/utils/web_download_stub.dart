// Stub para plataformas no-web (macOS, Windows, Linux, iOS, Android).
// La función no hace nada; el caller debe gestionar la lógica de guardado.
void downloadFileOnWeb(List<int> bytes, String fileName) {}
