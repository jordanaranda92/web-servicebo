import 'package:flutter/material.dart';

/// Color schemes for the app following Material Design 3.
///
/// Uses [ColorScheme.fromSeed] to generate harmonious color palettes
/// automatically from a seed color.
class AppColorSchemes {
  /// Seed color for generating Material 3 color schemes.
  static const Color seedColor = Colors.blueAccent;
}
