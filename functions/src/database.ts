/**
 * Firestore database that all Cloud Functions operate on.
 *
 * The data lives in the named "servicebo-main" database
 * (europe-southwest1), not the project's "(default)" database.
 */
export const DATABASE_ID = "servicebo-main";
